# Assignment #1 ~ First Node project

Debug the following JS functions in the `index.js` file, to make sure they output the result indicated

## Running the script

Run this command in the terminal: `node index.js`
